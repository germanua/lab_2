/**
 * Interface for graphic objects.
 */
public interface GraphicObject {
    /**
     * Method to draw the graphic object at specified coordinates.
     *
     * @param x The x-coordinate.
     * @param y The y-coordinate.
     */
    void draw(int x, int y);
}
